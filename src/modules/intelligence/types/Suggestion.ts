export type Suggestion = {
  ball_name: string;
  catch_rate: string;
  average_throws: string;
  cost_per_catch: number;
  projected_profit: number;
  profit_per_throw: number;
  roi_percent: string;
  is_recommended: boolean;
};
